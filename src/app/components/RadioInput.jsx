export default function RadioInput() {
  <label className="flex justify-between" key={key} htmlFor={htmlFor}>
    {text}
    <input type="radio" id={htmlFor} name="area" value="area" />
  </label>;
}
